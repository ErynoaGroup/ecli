# Work ROADMAP

Erynoa Docs **v1**. Status: `draft` · `active` · `done` · `dropped` (frozen).  
Trunk-based integration → merge to **`main`**. IDs never reused.

**Führend:** this file under `docs/work/` is the ROADMAP SSOT for `ecli`.

| ID | Title | Status | Path | Notes |
|----|-------|--------|------|-------|
| BOOT-01 | Work-root scaffold (`ROADMAP` · `specs/` · `plans/`) | `active` | docs/work/ | empty-repo catch-up after seed `a9d9ac6`; no product SPEC invented |

No product SPEC in this remediation (BOOT only). Product scope stays undefined until an owner writes one.
